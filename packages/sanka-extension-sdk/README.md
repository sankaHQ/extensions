# Sanka Extension SDK

The dependency-free Apache-2.0 contract for Sanka migration extension subprocesses.

Extensions receive one `sanka-extension/v1` JSON request on standard input and write one
validated response to standard output. Diagnostics belong on standard error.
