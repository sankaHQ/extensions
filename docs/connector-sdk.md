# Sanka Extension SDK

Use the [Sanka Extension SDK guide](../packages/sanka-extension-sdk/README.md)
for the canonical `sanka_extensions.data` and `sanka_extensions.code` interfaces.
See [Building Extensions](extension-development.md) for implementation guidance.

This documentation path remains available for existing links. Published import
and package identifiers are listed in the [compatibility guide](naming-compatibility.md).
